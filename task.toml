version = "1.0"

[metadata]
category = "Kernel-Framework Integration"
secondary_categories = ["Tensor memory/Layout"]
tags = ["pytorch", "custom-op", "strides", "torch.compile", "sparse-ops"]
difficulty = "hard"
difficulty_explanation = "The task requires fixing a custom C++ operator performing sparse index gather and scaling that fails on non-contiguous weight matrices and lacks meta/fake tensor registration for torch.compile. The agent must implement correct multi-dimensional stride-aware memory access and register the matching meta function."
solution_explanation = "Update the custom C++ kernel to compute logical multi-dimensional offsets using input strides rather than assuming contiguous layout, and register a meta kernel for torch.compile fullgraph compatibility."
expert_time_estimate_hours = 4.0

[verifier]
timeout_sec = 900
reward_type = "bounded_continuous"

[agent]
timeout_sec = 900

[environment]
build_timeout_sec = 900
cpus = 2
memory_mb = 4096
storage_mb = 10240
